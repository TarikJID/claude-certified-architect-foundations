# Evaluation — domain-researcher (Tool Design & MCP Integration) — round 1

- Output evaluated: `runs/claude-certified-architect-foundations/research/tool-design-mcp-integration.md`
- Checklist: domain-researcher's "Done when" checklist
- Source of truth used: the sources cited in the output itself. Fetched: `https://platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools`, `https://www.anthropic.com/engineering/writing-tools-for-agents` (fetched twice, second time searching the full text specifically for the disputed quote/stat), `https://modelcontextprotocol.io/specification/2025-06-18/server/tools`, `https://code.claude.com/docs/en/mcp` (fetched twice, second time searching specifically for the "Tool Discovery and Availability" section), `https://code.claude.com/docs/en/tools-reference`, `https://www.anthropic.com/engineering/built-multi-agent-research-system`, `https://code.claude.com/docs/en/best-practices`, `https://modelcontextprotocol.io/specification/2025-06-18/server/resources`, `https://github.com/modelcontextprotocol/servers`. Also cross-checked bullet IDs against `runs/claude-certified-architect-foundations/dispatch/tool-design-mcp-integration.md` and `runs/claude-certified-architect-foundations/sources/exam-guide.txt`.
- Repeat round: no
- Verdict: REWORK

## Checklist results

| # | Checklist item | Result | Evidence or issue |
|---|----------------|--------|-------------------|
| 1 | Every bullet given has at least one concept that teaches it; state bullets received/covered | PASS | Dispatch slice has 43 bullets (20 K + 23 S, IDs 2.1-K1..2.5-S5). Every one of the 43 IDs appears in at least one `Teaches:` field across the 24 key + 5 prerequisite concepts (traced ID-by-ID). Header states "Bullets received: 43 ... Bullets covered: 43" — matches. |
| 2 | Every count you state matches the file | PASS | Header claims "Concepts: 29 (24 key, 5 prerequisite)". Counted 24 `- Concept:` entries under "## Key Concepts" (lines 11–172) and 5 under "## Prerequisites" (lines 183–211). 24+5=29. Matches exactly. |
| 3 | Every key concept is defined, illustrated with a concrete example, and attributed to a source | FAIL | All 24 key concepts have Definition/Example/Source fields present (structural pass), but two attributions are not faithful to the source they cite: (a) "Tool description ambiguity/overlap and the rename/split fix" (line 21) attributes a specific quote — "Bad tool descriptions can send agents down completely wrong paths, so each tool needs a distinct purpose and a clear description" — and a specific statistic — "a tool-testing agent to rewrite ambiguous descriptions produced a measured 40% decrease in task completion time" — to `https://www.anthropic.com/engineering/writing-tools-for-agents`. Two independent fetches of that page (including one searching the full text specifically for "40%", "tool-testing agent", "task completion time", and the exact quote) found none of this content anywhere on the page. This is a fabricated attribution to a real official source. (b) "Simultaneous discovery of tools across all configured MCP servers" (line 109) cites `https://code.claude.com/docs/en/mcp` for a "Tool Discovery and Availability" section stating that Claude Code discovers tools from all connected MCP servers simultaneously at session start. A targeted fetch found no such section heading on that page, and the page's actual content on server connection (tool-search caching, servers connecting lazily/asynchronously, tools becoming available "on Claude's next request in the same turn" after a server finishes connecting) describes something closer to asynchronous/lazy discovery, not the simultaneous-at-session-start behavior the concept asserts and cites the page for. |
| 4 | Each key concept's immediate prerequisites are identified and defined the same way | PASS | 5 prerequisite concepts present (Tool definition structure; agentic tool-use loop; MCP client-server architecture; coordinator-subagent architecture; Claude Code built-in tools), each with Definition/Example/Source, each referenced via its own `Teaches:` field pointing back at the key-concept bullets that depend on it. |
| 5 | Every cited source meets the quality bar (authoritative/official — no forums, social, or blogs) | PASS | All sources cited are: the exam guide, `platform.claude.com` API docs, `code.claude.com` Claude Code docs, `modelcontextprotocol.io` spec, Anthropic's own engineering blog (`anthropic.com/engineering/...`), and the official `github.com/modelcontextprotocol/servers` repo. No forum/social/blog sources used. |
| 6 | No concept cites non-official where official exists; official sources not ranked | PASS | All citations are to official sources per the researcher's own brief (exam guide, product docs, Anthropic engineering blog, MCP spec). No finding raised over which official source was chosen. |
| 7 | Any unsourced concept carries `Status: UNSOURCED` + `Searched:` record | N/A / PASS | Header states 0 UNSOURCED and no concept in the file carries that status; all 43 bullets are covered by sourced concepts, consistent with the self-report. |

## Persisting issues

N/A — this is round 1, not a repeat round.

## Not checked

- Not every one of the ~15 distinct cited URLs across all 29 concepts was independently fetched; the spot-check above covered the majority of distinct sources (10 of the cited URLs) including every source used more than once. The remaining single-use citations (e.g., the `implement-tool-use` page for `tool_choice`) were not independently fetched, but their claimed content (the `tool_choice` auto/any/tool/none semantics and forced-tool prefill behavior) was independently corroborated via the `define-tools` page, which documents the same API behavior in near-identical terms, so this was treated as adequately verified in substance rather than fetched separately.

## Why this is REWORK, not a nitpick

Two of 24 key concepts (Concept 2 and Concept 15) cite real, official sources for specific claims — a direct quote and a 40% statistic in one case, a named section and a discovery-timing claim in the other — that a full-text search of those pages does not support. This is exactly the failure mode the checklist's "attributed to a source" requirement exists to catch: a reader trusting the citation would be misled about what the official source actually says. This is a researcher defect (fabricated/inaccurate sourcing), not a stylistic issue, and should be routed back to the domain-researcher to correct or re-source those two concepts.
