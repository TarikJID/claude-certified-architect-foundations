# Evaluation — domain-researcher (Domain 1: Agentic Architecture & Orchestration) — round 1

- Output evaluated: `runs/claude-certified-architect-foundations/research/agentic-architecture-orchestration.md`
- Checklist: domain-researcher's "Done when" checklist
- Source of truth used: the sources cited in the output itself — fetched and cross-checked:
  - https://code.claude.com/docs/en/agent-sdk/agent-loop
  - https://code.claude.com/docs/en/agent-sdk/subagents
  - https://code.claude.com/docs/en/agent-sdk/hooks
  - https://code.claude.com/docs/en/agent-sdk/permissions
  - https://code.claude.com/docs/en/agent-sdk/sessions
  - https://code.claude.com/docs/en/mcp
  - https://platform.claude.com/docs/en/agents-and-tools/tool-use/build-a-tool-using-agent
  - https://www.anthropic.com/research/building-effective-agents
  - https://www.anthropic.com/engineering/multi-agent-research-system
  - `runs/claude-certified-architect-foundations/sources/exam-guide.txt` (lines 150-284, 960-989, cited line ranges)
  - `runs/claude-certified-architect-foundations/dispatch/agentic-architecture-orchestration.md` (the 48 bullets and IDs)
- Repeat round: no
- Verdict: **REWORK**

## Checklist results

| # | Checklist item | Result | Evidence or issue |
|---|----------------|--------|-------------------|
| 1 | Every bullet given has at least one concept teaching it; state bullets received/covered | PASS | Header states "Bullets received: 48 ... Bullets covered: 48 (48/48)". Manually traced every bullet ID (1.1-K1..1.7-S4, 48 total, matching the dispatch slice's IDs) against the `Teaches:` fields of all 34 concepts. Every one of the 48 IDs appears in at least one `Teaches:` field (e.g. 1.3-K4 is taught by "Session Forking for Divergent Exploration" even though that concept sits in the 1.7 section — correctly cross-referenced in the Coverage summary). Coverage summary table's per-task counts (6+8+8+6+6+6+8=48) also check out. |
| 2 | Every stated count matches the file | PASS | Header claims "34 concepts (30 key, 4 prerequisite)". Manually counted `Concept:` entries: 4 under "Prerequisite concepts", 4 under 1.1, 6 under 1.2, 6 under 1.3, 4 under 1.4, 2 under 1.5, 4 under 1.6, 4 under 1.7 = 30 key + 4 prerequisite = 34 total. Matches exactly. |
| 3 | Every key concept defined, illustrated with a concrete example, attributed to a source | MOSTLY PASS, one adjacent issue noted under #4 | All 30 key concepts have `Definition:`, `Example:`, and `Source:` fields populated. Spot-verified several against live sources: the "Agentic Loop Lifecycle" concept's worked example ("Fix the failing tests in auth.ts", turn-by-turn Bash/Read/Edit) is a near-verbatim match to the actual example in the fetched `agent-loop` doc. "PostToolUse Hooks" and "PreToolUse Hooks" concepts match the fetched `hooks` doc's `updatedToolOutput`/`updatedMCPToolOutput` (deprecated) and `permissionDecision`/`permissionDecisionReason` fields exactly. "Permission evaluation order" concept's six-step order (hooks → deny → ask → permission mode → allow → canUseTool) and the "hook deny overrides bypassPermissions" claim match the fetched `permissions` doc verbatim. "AgentDefinition Configuration" concept's field list matches the fetched `subagents` doc's config table. `Hub-and-Spoke`, `Coordinator Responsibilities`, `Risks of Overly Narrow Task Decomposition`, `Parallel Subagent Spawning` (3-5 subagents, 90% time reduction), and `Structured Data Formats`/CitationAgent concepts were all confirmed present and accurately paraphrased in the fetched `multi-agent-research-system` engineering post. `Model-Driven Decision-Making vs Pre-configured Decision Trees` and `Prompt Chaining`/`Dynamic Adaptive Decomposition` concepts match the fetched `building-effective-agents` post's workflows-vs-agents and orchestrator-workers framing. Exam-guide line citations (160-162, 190-191, 210-214, 222-225, 234-236, 250-262, 267-268, 271-272, 282-283, 976) were all read directly and match the quoted/paraphrased bullet text exactly. One soft concern: the key concept "The Agentic Loop Lifecycle (`stop_reason`-driven control flow)" (1.1-K1/S1) attributes the specific mechanic "the loop's continuation decision is driven entirely by the `stop_reason` field... `tool_use`... `end_turn`" solely to the `agent-loop` SDK doc, but that doc (as currently published) frames loop continuation via message types (`AssistantMessage`/tool-call presence) rather than a per-turn `stop_reason` value, and its own `stop_reason` discussion is scoped to the *final* turn with a different value set (`end_turn`, `max_tokens`, `refusal`). The `tool_use`/`end_turn` stop_reason framing is accurate and well-grounded (it is the exam guide's own K1 wording, verbatim, and matches the raw Messages API per the prerequisite concept's own citation), but it is not actually what the cited SDK page says — a citation-fidelity gap, not a factual error. Not treated as a standalone fail since it doesn't affect a checklist item on its own, but recorded here as it bears on #4's severity assessment. |
| 4 | Each key concept's immediate prerequisites identified and defined the same way (definition, example, source) | FAIL | All 4 prerequisite concepts have `Definition:`, `Example:`, and `Source:`. However, one is mis-attributed: the prerequisite "MCP tool results and the `isError` pattern" (feeding 1.5-K1, 1.5-S1) cites `https://code.claude.com/docs/en/mcp (MCP tool results/isError conventions)`. Two independent full-text fetches of that page, one of them explicitly searching for the string "isError", confirm the term **does not appear anywhere on that page**, nor does the page discuss heterogeneous MCP result formats/shapes across servers (the concept's core claim). The page covers installing/configuring MCP servers, auth, tool management, and output-size limits only. The concept's secondary citation — the exam guide's Appendix line 976 — only lists "isError flag" as a bare term in a list of "Technologies and Concepts that might appear on the exam," with zero elaboration; it does not substantiate the concept's actual definitional content (normalization need, heterogeneous timestamp/status-code example). So the concept's substantive claims are not actually backed by either cited source. |
| 5 | Every cited source meets the quality bar (authoritative/official) | PASS (on source-type grounds), but see #4 | All cited domains — code.claude.com/docs, platform.claude.com/docs, anthropic.com/research, anthropic.com/engineering, and the archived exam guide — are official per the researcher's own brief. No forum/social/blog citations found. The #4 issue is a faithfulness problem (source doesn't say what's claimed), not a source-quality problem (the source itself is legitimately official). |
| 6 | No concept cites non-official where official exists; non-official citations justified | PASS | No non-official sources were cited anywhere in the file, so this item does not trigger. |
| 7 | Any unsourced concept carries `Status: UNSOURCED` + `Searched:` record | Not directly violated, but see #4 | No concept in the file is marked `UNSOURCED`, and the self-report is "0 UNSOURCED." Given the #4 finding, the `isError` prerequisite concept's source does not actually hold up under verification; the concept needed either a real source (e.g., the MCP specification, which does define `isError`, and which the researcher never cited) or should have carried the `UNSOURCED`/`Searched:` escape hatch the checklist provides for exactly this situation. |

## Persisting issues

Not applicable — this is round 1, not a repeat round.

## Not checked

- The two Domain 2/Appendix corroborating exam-guide citations (lines 976 for isError, and general Appendix content) were checked directly in the file; no other exam guide line ranges beyond the ones cited in the output were separately audited.
- Did not independently verify the MCP specification (modelcontextprotocol.io) content, since it was not cited by the output — only used to confirm that an official alternative source for `isError` does exist and was available for the researcher to use.
- Did not re-verify every one of the ~19 distinct source URLs cited across all 34 concepts line-by-line beyond the spot checks listed in row 3/4 above; the sample covered every distinct source domain used in the file and both exam-guide-only citations, which gives reasonable confidence the remaining citations of the same sources are similarly reliable, but each individual citation was not independently fetched.

## Recommendation

Send back to the researcher specifically for the "MCP tool results and the `isError` pattern" prerequisite concept: either re-source it to a document that actually contains this content (e.g., the Model Context Protocol specification, which defines `isError` on tool results), or mark it `Status: UNSOURCED` with a `Searched:` record per the checklist's own provision. The rest of the file's sourcing is strong and does not need rework.
