# Evaluation — domain-researcher (Claude Code Configuration & Workflows) — round 1

- Output evaluated: `runs/claude-certified-architect-foundations/research/claude-code-configuration-workflows.md`
- Checklist: domain-researcher's "Done when" checklist
- Source of truth used: the sources this output itself cites — fetched `https://code.claude.com/docs/en/memory`, `https://code.claude.com/docs/en/slash-commands`, `https://code.claude.com/docs/en/skills`, `https://code.claude.com/docs/en/best-practices` (fetched twice, including a full-page re-check targeted at the TDD claim), `https://code.claude.com/docs/en/sub-agents`, `https://code.claude.com/docs/en/headless`, `https://code.claude.com/docs/en/github-actions`, `https://code.claude.com/docs/en/permission-modes`, `https://code.claude.com/docs/en/common-workflows`, `https://code.claude.com/docs/llms.txt`, plus the dispatch slice (`runs/claude-certified-architect-foundations/dispatch/claude-code-configuration-workflows.md`) and the archived exam guide (`runs/claude-certified-architect-foundations/sources/exam-guide.txt`).
- Repeat round: no
- Verdict: REWORK

## Checklist results

| # | Checklist item | Result | Evidence or issue |
|---|----------------|--------|-------------------|
| 1 | Every bullet given has at least one concept that teaches it, named in `Teaches:`, with a stated bullets-received/covered count | PASS | Header states 49 received / 49 covered. Traced all 49 bullet IDs (3.1-K1..S4, 3.2-K1..S5, 3.3-K1..S3, 3.4-K1..S4, 3.5-K1..S5, 3.6-K1..S5) against the dispatch slice and confirmed each appears in at least one concept's `Teaches:` field (e.g. 3.1-S4 → concept 5; 3.4-S4 → concept 21; 3.6-S4 → concept 31). No gaps found. |
| 2 | Every count stated matches the file (independently re-counted) | PASS | Header claims 37 concepts (31 key + 6 prerequisite). Manually counted concept headings by task statement: 3.1=5, 3.2=7, 3.3=4, 3.4=5, 3.5=4, 3.6=6 → 31 key concepts. Prerequisites section = 6. Total 37, matches header. The file's own "Summary of bullet coverage" section (lines 327-335) also cross-checks correctly against these counts. |
| 3 | Every key concept is defined, illustrated with a concrete example, and attributed to a source | REWORK (one concept fails) | 30 of 31 key concepts checked against their cited source and matched (see below). One concept fails: **"Test-driven iteration"** (concept 23, teaches 3.5-K2/S2) cites `https://code.claude.com/docs/en/best-practices` ("Give Claude a way to verify its work" — tests as a check Claude can run and iterate against until it passes), but its Definition asserts a specific "recommended workflow: write the tests, confirm they fail, optionally commit the failing tests as a checkpoint, then have Claude implement until they pass." I fetched the full best-practices.md page twice (once with a targeted prompt asking specifically whether this red/green/commit sequence appears anywhere on the page) and confirmed it does not appear anywhere on that page, in that section or any other. The page's actual content on tests is generic verification guidance (give Claude a check it can run: tests, a build, a screenshot) with no "confirm failure first" / "commit failing tests" procedure. This specific content is not present in the cited source. (It resembles guidance from an older, different Anthropic blog post, not the cited page.) Unlike two other concepts in this file (3.5-K4/S5 and 3.6-S4), where the researcher explicitly flagged that it could not find product-doc coverage and fell back to the exam guide with a stated justification, this concept presents an unverifiable, specific procedural claim as if drawn directly from the cited official page, with no such flag. All other 30 key concepts were spot-checked against fetched source text and matched (CLAUDE.md hierarchy/table, @import syntax, .claude/rules/, /memory and /context, SKILL.md frontmatter fields incl. context:fork/allowed-tools/argument-hint, project vs personal commands, path-specific rules paths field and glob table, plan mode activation and "explore/plan/implement" workflow, Explore subagent read-only behavior, interview pattern, -p/--print and exit codes, --output-format json/--json-schema, CLAUDE.md for CI context, session isolation for review, "skips PRs with existing Claude comment"). |
| 4 | Each key concept's immediate prerequisites are identified and defined the same way (definition, example, source) | PASS | All 6 prerequisite concepts (Context window/token budget, YAML frontmatter, Subagents and isolated context, Tool permission approval/allowedTools, JSON Schema specification, Non-interactive scripting/CI pipeline steps) have Definition, Example, and Source fields, and each states which key concepts it is prerequisite for. Spot-checked "Subagents and isolated context" and "Context window and token budget" against fetched sub-agents.md and memory.md text — both match. |
| 5 | Every cited source meets the quality bar (authoritative/official — no forums, social, or blogs) | PASS | All citations are to `code.claude.com/docs/en/*` pages, the exam guide itself, or `json-schema.org` (a primary specification). No forum, social, or blog citations found. |
| 6 | No concept cites a non-official source where an official one exists; every non-official citation names the source and says why nothing official covers it; official sources not ranked | PASS (with the note above) | Two concepts (3.5-K4/S5 and 3.6-S4) are cited to the exam guide with an explicit one-line justification each explaining why no product-doc page covers that exact decision rule; per the task brief the exam guide is itself an official source, so this is not a defect and is not being treated as one. This is separate from the item-3 issue above, which is about content fidelity to a cited product-doc page, not about which official source category was chosen. |
| 7 | Any concept that could not be sourced carries `Status: UNSOURCED` and a `Searched:` record | PASS | Researcher self-reports 0 UNSOURCED concepts. Confirmed no concept in the file carries an UNSOURCED status; every concept has a populated Source field pointing to either a product-doc page, the exam guide (with justification), or json-schema.org. |

## Persisting issues

Not applicable — this is round 1, not a repeat round.

## Not checked

- The `-p` flag detail "Plan mode is activated with... by prefixing a prompt with /plan" (concept 17) is cited to best-practices.md but that exact `/plan` prefix detail actually lives on `permission-modes.md` ("Enter plan mode by pressing Shift+Tab or prefixing a single prompt with /plan"), which I did fetch and confirms the fact is true and officially documented, just on a different official page than the one cited. Per the brief, official sources are not ranked against each other and this is a true, sourced-elsewhere fact rather than a fabrication, so I did not treat it as a defect — flagging only for visibility since it's adjacent to the item-3 issue above.
- I did not independently re-verify every single example (only definitions/sources) against the fetched pages line-by-line for all 37 concepts; I spot-checked representative concepts across all six task statements plus every concept whose wording looked most specific/quotable, and fetched every unique source URL cited in the file.

## Summary for rework

The single required fix is concept 23 ("Test-driven iteration," teaching 3.5-K2/S2): either (a) revise the Definition to only claim what `code.claude.com/docs/en/best-practices` actually supports (generic "give Claude a check it can run" verification framing, without the specific write→confirm-fail→commit→implement sequence), or (b) if that specific red/green/commit workflow is the intended teaching content, cite a source that actually contains it, or fall back to the exam guide with the same "non-official, here's why" justification pattern already used elsewhere in this file for 3.5-K4/S5 and 3.6-S4.
