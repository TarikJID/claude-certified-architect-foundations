# Evaluation — domain-researcher (Tool Design & MCP Integration) — round 2

- Output evaluated: `runs/claude-certified-architect-foundations/research/tool-design-mcp-integration.md`
- Checklist: domain-researcher's "Done when" checklist
- Source of truth used: the sources cited in the output itself, with focused re-verification of the two concepts flagged in round 1. Fetched: `https://www.anthropic.com/engineering/writing-tools-for-agents` (full-text search for the quote "When tools overlap in function or have a vague purpose, agents can get confused about which ones to use.", the "Namespacing your tools" / "Choosing the right tools for agents" section headings, "grouping related tools under common prefixes" / "can help delineate boundaries between lots of tools", "more tools don't always lead to better outcomes", "distract agents from pursuing efficient strategies", and a search confirming "40%", "task completion time", "tool-testing agent" are absent); `https://code.claude.com/docs/en/mcp` (full-text search for the "Tool availability" subsection under "Scale with MCP tool search", the "Server status detail" section, "connects on first use", "connects the server the first time Claude calls one of the server's tools", "Claude waits for that server before continuing", the `WaitForMcpServers` tool, and confirmation that no "Tool Discovery and Availability" section exists). Also cross-checked the dispatch slice (`runs/claude-certified-architect-foundations/dispatch/tool-design-mcp-integration.md`) bullet-by-bullet against every `Teaches:` field in the output, and recounted concept headers directly.
- Repeat round: yes — round 1 verdict at `runs/claude-certified-architect-foundations/evaluations/domain-researcher-tool-design-mcp-integration-round-1.md`
- Verdict: PASS

## Checklist results

| # | Checklist item | Result | Evidence or issue |
|---|----------------|--------|-------------------|
| 1 | Every bullet given has at least one concept that teaches it; state bullets received/covered | PASS | Dispatch slice lists 43 bullets across 2.1–2.5 (8+8+9+9+9). Traced every ID against the 24 key + 5 prerequisite concepts' `Teaches:` fields; all 43 appear at least once (e.g. 2.1-K1–K4/S1–S4 fully covered by Concepts 1–3; 2.4-K1–K4/S1–S5 fully covered by Concepts 13–18). Header states "Bullets received: 43 ... Bullets covered: 43" — matches. |
| 2 | Every count you state matches the file | PASS | Header claims "Concepts: 29 (24 key, 5 prerequisite)". Counted 24 `- Concept:` entries under "## Key Concepts" and 5 under "## Prerequisites" — 24+5=29, matches exactly. |
| 3 | Every key concept is defined, illustrated with a concrete example, and attributed to a source | PASS | All 24 key concepts have Definition/Example/Source present. Specifically re-verified the two concepts flagged as fabricated in round 1: (a) "Tool description ambiguity/overlap and the rename/split fix" (line 18) no longer contains the fabricated quote or the "40% decrease in task completion time" statistic; it now quotes "When tools overlap in function or have a vague purpose, agents can get confused about which ones to use." and cites the "Namespacing your tools" / "Choosing the right tools for agents" sections and the phrase "grouping related tools under common prefixes ... can help delineate boundaries between lots of tools" — all confirmed present verbatim on `https://www.anthropic.com/engineering/writing-tools-for-agents`. A full-text search of that page for "40%", "task completion time", and "tool-testing agent" confirms none of that fabricated material is on the page (consistent with it having been removed). (b) The concept formerly "Simultaneous discovery of tools across all configured MCP servers" (round 1, line 109) citing a nonexistent "Tool Discovery and Availability" section is now retitled "Tools from connected MCP servers available together to the agent" (line 109) and cites "Tool availability" under "Scale with MCP tool search" and "Server status detail" on `https://code.claude.com/docs/en/mcp` — both confirmed as real section headings on the page — along with the quoted phrases "connects on first use," "connects the server the first time Claude calls one of the server's tools," and "Claude waits for that server before continuing," all confirmed verbatim, plus a confirmed reference to the `WaitForMcpServers` tool. The rewritten definition is also now appropriately hedged (asynchronous/lazy connection, waiting behavior) rather than asserting an unsupported "simultaneous at session start" claim. |
| 4 | Each key concept's immediate prerequisites are identified and defined the same way | PASS | 5 prerequisite concepts present (Tool definition structure; agentic tool-use loop; MCP client-server architecture; coordinator-subagent architecture; Claude Code built-in tools), each with Definition/Example/Source, each referenced via its own `Teaches:` field. Unchanged from round 1 and not implicated in either flagged issue. |
| 5 | Every cited source meets the quality bar (authoritative/official — no forums, social, or blogs) | PASS | All sources cited are the exam guide, `platform.claude.com` API docs, `code.claude.com` Claude Code docs, `modelcontextprotocol.io` spec, Anthropic's engineering blog, and the official `github.com/modelcontextprotocol/servers` repo. No forum/social/blog sources. |
| 6 | No concept cites non-official where official exists; official sources not ranked | PASS | All citations are to official sources per the researcher's brief. No finding raised over which official source was chosen. |
| 7 | Any unsourced concept carries `Status: UNSOURCED` + `Searched:` record | PASS | Header states 0 UNSOURCED and no concept in the file carries that status; all 43 bullets are covered by sourced concepts. |

## Persisting issues

Both issues raised in round 1 are resolved:
- The fabricated quote and "40% decrease in task completion time" statistic on Concept 2 ("Tool description ambiguity/overlap and the rename/split fix") — no longer present; replaced with a verbatim, verified quote from the cited source.
- The fabricated "Tool Discovery and Availability" section citation on the MCP tool-discovery concept — the concept has been retitled and re-sourced to real, verified section headings ("Tool availability" / "Server status detail") with verbatim-verified supporting quotes, and the claim itself was softened to match what the source actually documents.

No other issue from round 1 remains open (round 1 raised no other findings).

## Not checked

- Not every one of the ~15 distinct cited URLs across all 29 concepts was re-fetched in this round; per the task instructions, verification was focused on the two concepts round 1 flagged as fabricated. The remaining sources were spot-checked in round 1 and are unchanged in this revision (the researcher reports only Concepts 2 and the MCP-discovery concept were edited), so they were not re-verified here.
